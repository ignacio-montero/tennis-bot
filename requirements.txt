# Pinned so the Docker base image (mcr.microsoft.com/playwright/python:v1.60.0-noble)
# ships the exact matching browser build — see deploy/docker/Dockerfile.
playwright==1.60.0
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
structlog>=24.1
